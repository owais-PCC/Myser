<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>Myserly</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!-- Receipt capture goes through a hidden <input type="file" accept="image/*">
	     (src/components/BottomNav.tsx), which WKWebView presents as a sheet
	     offering Photo Library / Take Photo / Choose File. "Take Photo" reaches
	     the camera, and iOS terminates any app that does so without a purpose
	     string — a hard crash, not a denied-permission dialog. The library route
	     uses PHPickerViewController and needs nothing, but @capacitor/camera is
	     compiled into this target and would require the photo-library string the
	     moment it is called, so both are declared here. -->
	<key>NSCameraUsageDescription</key>
	<string>Myser uses the camera to photograph your receipts and read the amount, date, and merchant automatically.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Myser needs access to your photos so you can scan a receipt you have already photographed.</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLSchemes</key>
			<array>
				<!-- REVERSED_CLIENT_ID from GoogleService-Info.plist. Required
				     so iOS routes Google Sign-In's OAuth redirect back into
				     this app; without it GIDSignIn's completion handler never
				     fires and the sign-in flow hangs forever with no error. -->
				<string>com.googleusercontent.apps.566369077547-cshs5fvo4nn01hn22a4vp2k9b98seenf</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
